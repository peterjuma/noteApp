feat: lazy-loading, 66 component tests, bundle size reduction

Lazy Loading (-143KB main bundle):
- Mermaid: dynamic import in NoteMain, loads only when diagram blocks exist
- SettingsPanel: React.lazy() + Suspense, loaded on Settings open
- VersionHistory: React.lazy() + Suspense, loaded on history open
- TableConverter: React.lazy() + Suspense, loaded on TC open
- Main bundle reduced from 899KB to 756KB (-16%)

Component Tests (27 new, 66 total):
- NavbarSidebar.test.js: rendering, title states, button interactions (11 tests)
- NavbarMain.test.js: toolbar buttons, callbacks, conditional rendering (8 tests)
- ConfirmDialog.test.js: rendering, confirm/cancel/secondary actions (6 tests)
- NoteSort.test.js: sort dropdown, selection change callback (3 tests)

Dev Dependencies:
- Added @testing-library/dom (peer dep)
- Added @babel/plugin-proposal-private-property-in-object (CRA warning fix)

Note: App.js class-to-function migration deferred — requires comprehensive
integration test coverage first to prevent regressions in 1500-line component.
