feat: 79 tests (13 App integration), React 18 defensive guards

App.js Integration Tests (13 tests):
- Initial render: sidebar, sort dropdown
- Navigation: home button, settings open, TC open
- Editor: new note creation, editor rendering
- Settings: dark mode toggle persistence, General content
- Search: toggle shows input
- Sidebar: collapse/expand, localStorage persistence
- Navigation guards: confirm dialog on editor switch, Keep Editing

React 18 Compatibility Fixes:
- Defensive fallbacks in render: allnotes, pinnedNotes, filteredNotes, workspaces
- componentDidMount callback uses local variable instead of state
- Prevents crashes during React 18 strict mode double-mount lifecycle

Test Infrastructure:
- Full mock layer: notesDB, gistSync, NoteEditor, SettingsPanel, VersionHistory, TableConverter
- Mock fetch for README loading
- Mock clipboard API
- 79 total tests across 9 suites, all passing

Note: App.js class-to-functional migration deferred to separate session.
Requires the integration test coverage now in place to be safe.
