feat: GitHub Gist sync for cross-device backup

Sync Service (src/services/gistSync.js):
- Push notes to a private GitHub Gist (one per workspace)
- Pull notes from Gist with newest-wins merge strategy
- Full bidirectional sync (pull + merge + push)
- Token validation against GitHub API
- Link existing Gist by ID
- Last sync timestamp tracking per workspace

Settings > Sync Tab:
- GitHub Personal Access Token input with show/hide
- Verify button validates token and shows GitHub username + avatar
- Enable Sync toggle for automatic background sync
- Sync Now button with spinner animation and status messages
- View Gist link opens the Gist on GitHub
- Link Existing Gist input for connecting to a previous sync

Auto-Sync:
- Background push after every save (10-second debounce)
- Non-blocking, silent failure for background operations
- Only active when sync is enabled and token is configured

Merge Strategy:
- Per-note newest-wins based on updated_at timestamp
- New remote notes are added to local
- New local notes are pushed to remote
- Conflicts within 1 minute flagged (but resolved automatically)

New file: src/services/gistSync.js
